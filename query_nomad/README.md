the script queries the v1 `/entries/query` API for *all* visible ORCA entries

builds stratified random samples of specified sizes

writes each sample to both JSON and CSV files

selection controlled by a fixed random seed to be reproducible
